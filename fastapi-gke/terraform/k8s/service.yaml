apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  labels:
    app: fastapi-backend
spec:
  type: LoadBalancer
  selector:
    app: fastapi-backend
  ports:
    - name: http
      port: 80        
      targetPort: 8081
      nodePort: 30080
