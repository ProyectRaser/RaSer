apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  labels:
    app: fastapi-backend
    release: kube-prometheus-stack
spec:
  type: NodePort
  selector:
    app: fastapi-backend
  ports:
    - name: http
      port: 80
      targetPort: 8081
      nodePort: 30080
